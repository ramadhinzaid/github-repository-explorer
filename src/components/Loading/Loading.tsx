import { HashLoader } from "react-spinners";

export default function Loading() {
  return (
    <div className="h-full flex flex-col items-center justify-center">
      <HashLoader id="react-spinners-HashLoader" color="#4299e1" />
    </div>
  );
}
